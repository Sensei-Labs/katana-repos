import Icon from '@/Components/Atoms/Loading/Icon';

import { LoadingWrapper } from './style';

const Loading = () => {
  return (
    <LoadingWrapper>
      <Icon />
    </LoadingWrapper>
  );
};

export default Loading;
